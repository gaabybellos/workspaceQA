# Copy this to .env.local when a registered project needs runtime values.
# Never commit .env.local.

# Target application URL used by discovery and browser-based planning.
BASE_URL=http://localhost:3000

# Add project-specific values only when needed.
# Runner-specific variables are documented by the selected runner after installation.
